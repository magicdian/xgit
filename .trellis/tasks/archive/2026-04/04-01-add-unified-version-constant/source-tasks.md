## 1. 版本来源统一

- [x] 1.1 将 `xgit/Cargo.toml` 的 `package.version` 更新为 `2604.1.2`
- [x] 1.2 新增或统一版本访问入口，内部读取 `CARGO_PKG_VERSION`（或等效 Cargo 元数据入口）
- [x] 1.3 清理重复版本定义，保证代码仅通过统一入口读取版本

## 2. CLI 版本信息接入

- [x] 2.1 在 CLI 命令构建逻辑中接入统一版本访问入口
- [x] 2.2 确保 `xgit --version`（及等效版本查询入口）输出来自 Cargo 版本
- [x] 2.3 补充或更新帮助文案中的版本相关说明，避免与 Cargo 版本不一致

## 3. 硬编码替换与收敛

- [x] 3.1 全局扫描代码中的版本号字面量引用并替换为统一版本访问入口
- [x] 3.2 清理重复的版本定义或临时字符串拼接逻辑
- [x] 3.3 校验所有版本读取路径都通过 Cargo 版本来源

## 4. 测试与验证

- [x] 4.1 新增测试：校验 CLI 版本输出与 `Cargo.toml package.version` 一致
- [x] 4.2 新增测试：修改 `package.version` 后，版本输出可同步更新（无额外常量改动）
- [x] 4.3 运行现有测试并确认本变更未引入回归

## 5. 文档与版本约定

- [x] 5.1 更新开发文档，说明版本号仅允许在 `Cargo.toml package.version` 中维护
- [x] 5.2 记录版本命名建议（`YYMM.DD.BuildNumber` 可作为语义格式，推荐落地写法为 `2604.1.2` 这类 Cargo 兼容格式）
