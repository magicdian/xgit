## 1. 扩充内置代码文件类型目录

- [x] 1.1 在 `xgit/src/code_file_types.rs` 中补齐现有五个语言分类的常见代码扩展名条目，并保持 `c_line_block` 映射不变
- [x] 1.2 更新代码文件类型选择器相关的中英文文案与摘要统计，确保新增条目能在 setup 中正确展示
- [x] 1.3 保持分类默认选择语义不变，确认新增的 C/C++ 条目会随默认分类一起启用，而非代码文件不会被纳入内置目录

## 2. 同步默认规则与注释匹配行为

- [x] 2.1 更新 `xgit/src/config.rs` 与 `xgit/config/default.toml` 中的默认 `annotate.file_rules`，使其与扩充后的内置目录保持一致
- [x] 2.2 验证 setup 的结构化选择与 `annotate.file_rules` 回写/回读仍然稳定，并继续保留未知自定义规则
- [x] 2.3 验证 `annotate` 对新增代码扩展名（如 `*.hpp`、`*.mjs`、`*.kts`）不再报“未匹配到文件规则”，同时 `Android.bp` 仍默认不识别

## 3. 测试与版本更新

- [x] 3.1 补充或更新 `code_file_types`、setup UI 与 annotate 相关测试，覆盖新增扩展名的选择、映射和默认排除行为
- [x] 3.2 将 `xgit/Cargo.toml`（及必要的锁文件/文档/版本测试）更新到 `2604.1.3`，并确保 CLI 版本输出继续来自 Cargo 版本来源
- [x] 3.3 运行相关回归验证，确认扩展后的代码文件目录、默认规则和版本输出全部符合新规范
