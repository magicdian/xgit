## 1. 配置模型与格式化上下文

- [x] 1.1 在 `xgit/src/config.rs` 与默认配置中新增 `annotate.date`、`annotate.old_code` 及其子结构，并补充配置合并/默认值测试。
- [x] 1.2 为 `{date}` 增加基于系统本地时间的格式化能力，默认输出 `yyyy-mm-dd`，并补充默认格式与自定义格式的单元测试。

## 2. 注释渲染与兼容优先级

- [x] 2.1 改造 `xgit/src/annotate.rs` 的运行时上下文与模板展开逻辑，使 `{date}` 使用格式化结果而不是 Unix 时间戳。
- [x] 2.2 将 `modify` / `del` 的旧代码注入改为显式 `annotate.old_code` 策略驱动，并实现“显式策略优先、未配置时保留 `{old}` / 兼容回退”的渲染规则与测试。

## 3. Setup 与回归验证

- [x] 3.1 扩展 `xgit setup` 的注释策略栏目，支持编辑日期格式、旧代码展示模式及其模式相关字段。
- [x] 3.2 更新相关 i18n 文案、保存逻辑与回归测试，验证 setup 落盘结果和 `xgit annotate` 的注释输出符合新的日期格式与旧代码策略模型。
