## 1. CLI 与配置基础

- [x] 1.1 引入语言资源加载、配置模型与配置合并依赖，并建立资源/配置目录结构
- [x] 1.2 将 CLI 启动流程改造为 bootstrap 解析 + 配置加载 + 运行时命令树构建
- [x] 1.3 为 help、错误提示和状态提示接入本地化消息键，移除最终展示文本的硬编码
- [x] 1.4 实现默认值、全局配置、项目配置、环境变量和 CLI 覆盖的统一解析器
- [x] 1.5 为命令执行接入功能使能开关，并保证 `help` 与 `setup` 始终可用
- [x] 1.6 清理命令与路径处理中的平台假设，统一使用跨平台进程与路径 API

## 2. Setup 交互界面

- [x] 2.1 新增 `xgit setup` 与 `xgit setup --project` 命令入口
- [x] 2.2 实现 Git 工作区定位与项目作用域校验逻辑
- [x] 2.3 用 ratatui 实现 menuconfig 风格设置界面与核心栏目导航
- [x] 2.4 支持保存全局配置到 `~/.xgit/config.toml`，保存项目配置到 `<git-root>/.xgit/config.toml`
- [x] 2.5 在设置界面中实现功能开关、push 设置占位项、身份信息、注释表单、引用类型和文件规则编辑

## 3. 规范化注释引擎

- [x] 3.1 设计并实现注释策略包模型（identity、form、reference kinds、policies、file rules）
- [x] 3.2 实现 `--staged` 默认变更源及 `include_untracked` 处理逻辑
- [x] 3.3 实现 `--latest-commit` 变更源及边界条件校验（根提交、merge commit、脏工作区）
- [x] 3.4 实现运行时上下文采集流程，用于收集 `reason`、引用类型和值等本次执行数据
- [x] 3.5 实现 `add` / `modify` / `del` 三类改动识别与注释块生成

## 4. 渲染器与命令集成

- [x] 4.1 实现 C-like 行注释块渲染器，覆盖 `.h`、`.c`、`.cpp`、`.java` 等首批文件类型
- [x] 4.2 建立文件规则到渲染器的分发表，并对未实现的渲染器给出明确提示
- [x] 4.3 新增注释规范化命令参数：默认 `--staged`、显式 `--latest-commit` 及 staged 相关覆盖项
- [x] 4.4 将 push 命令接入统一配置层与功能状态提示

## 5. 跨平台兼容性

- [x] 5.1 为配置路径、home 目录定位、文件规则匹配和 Git 调用补充跨平台兼容处理
- [x] 5.2 校验 `setup`、`push` 和注释规范化命令在 macOS、Linux、Windows 上的运行前提与错误提示
- [x] 5.3 建立 macOS、Linux、Windows 的构建与测试验证流程

## 6. 测试与文档

- [x] 6.1 为语言选择优先级、配置合并优先级和功能禁用行为补充单元测试
- [x] 6.2 为 setup 作用域定位和 Git 工作区错误场景补充测试
- [x] 6.3 为 staged/latest-commit 源选择、改动分类、策略表单和注释渲染补充单元测试
- [x] 6.4 为平台敏感逻辑增加针对性单元测试，确保 Windows 与 Unix 路径/进程差异被覆盖
- [x] 6.5 更新 README 和帮助示例，说明配置文件位置、语言切换、setup 用法、注释规范化流程与跨平台支持范围

## 7. 回归修正

- [x] 7.1 修正 setup 界面的内容区域文案本地化，确保在 `zh-CN` 下标题、字段标签与操作提示全部显示中文
- [x] 7.2 从公开 CLI 中移除 `--lang` 选项，并更新帮助输出、README 与相关测试
- [x] 7.3 将 setup 主页面导航改为“上下选择 + Enter 进入”，去除对 Tab 切换主页面的依赖
- [x] 7.4 实现 setup 子页面的 ESC 返回上一级行为，并补充对应状态机测试
- [x] 7.5 实现 setup 在有未保存修改时按 ESC 退出的保存确认流程，并补充确认分支测试

## 8. 注释落盘与模板补全

- [x] 8.1 让 `annotate` 在执行完成后真正把注释写回目标文件，而不是仅输出 preview
- [x] 8.2 在默认 `--staged` 模式下刷新 index，确保 `git diff --cached` 展示的是规范化后的结果
- [x] 8.3 在 `--latest-commit` 模式下把规范化结果物化到工作区或索引中，并保留手动 amend 提示
- [x] 8.4 让渲染器严格使用 `annotate.policies.*`、`identity.author_tag` 与运行时表单字段展开模板，移除硬编码调试格式
- [x] 8.5 将注释插入逻辑细化到 diff hunk / 实际改动块，确保 `add` / `modify` / `del` 包裹真实代码片段并记录 `old` 内容
- [x] 8.6 补充回归测试，验证 `annotate` 执行后文件内容与 staged/latest-commit 结果都会实际发生变化
