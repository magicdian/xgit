## 1. 注释块识别与待提交判定

- [x] 1.1 在 `xgit/src/annotate.rs` 中引入“基线内容 / 当前渲染内容 / 逻辑文件视图”所需的中间数据结构，并区分 staged 与 latest-commit 两种基线来源。
- [x] 1.2 基于当前生效的 `block_templates.add/modify/del` 与旧代码展示策略实现候选注释块识别能力，能够定位起始边界、结束边界和代码体范围。
- [x] 1.3 结合当前变更源相对基线的新增信息，实现“当前待提交注释块 vs 历史注释块”的判定逻辑。

## 2. 还原视图与净改动重建

- [x] 2.1 实现待提交 `add` / `modify` / `del` 注释块到逻辑文件视图的还原规则，确保 `add/modify` 保留代码体、`del` 从逻辑视图中移除。
- [x] 2.2 让 annotate 在渲染前基于“基线内容 vs 逻辑文件视图”重新计算净 diff，并复用现有渲染器生成新的注释结果。
- [x] 2.3 为无法可靠还原的块增加保守失败路径和明确错误提示，禁止在歧义状态下继续追加新的注释外壳。

## 3. 上下文复用与交互兜底

- [x] 3.1 基于当前起始模板的反向匹配能力提取待提交注释块中的 `reason`、`reference_kind`、`reference_value` 候选值。
- [x] 3.2 实现“唯一一致才提示复用、冲突或缺失则回退常规输入”的上下文复用策略，并补充相关 CLI/i18n 文案。

## 4. 状态回放与边界测试

- [x] 4.1 补充状态回放测试，覆盖 `add` 注释块在块内继续新增、删除、修改后再次 annotate 仍收敛为单一 `add` 的行为。
- [x] 4.2 补充状态回放测试，覆盖 `modify` / `del` 注释块在同一批待提交改动中再次 annotate 时的还原与重建行为。
- [x] 4.3 补充边界测试，验证历史注释块不参与回滚、latest-commit 以 `HEAD^` 为基线、多个上下文冲突时不得自动复用。
- [x] 4.4 补充异常测试，覆盖模板差异、legacy old-code 回退、人工破坏注释边界、重叠块或无法判定代码体时的保守失败行为。
