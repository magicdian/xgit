## 1. 调整配置与状态建模

- [x] 1.1 在 `xgit/src/config.rs` 中为旧代码处理拆分“是否启用”和“使用何种模式”的状态表达，支持在禁用时保留最后一次有效模式。
- [x] 1.2 为旧代码处理补充历史配置兼容读取与保存语义，确保旧配置中的禁用写法仍能被正确加载。

## 2. 修正 setup 开关行为

- [x] 2.1 更新 `xgit/src/setup_ui.rs` 中 `新增格式`、`修改格式`、`删除格式` 的开关逻辑，使其在关闭和重新开启时只切换启用状态而不重置模板文本。
- [x] 2.2 更新 `xgit/src/setup_ui.rs` 中 `旧代码处理` 的开关逻辑，使其在重新开启时恢复关闭前最后一次有效模式，而不是固定回退到 legacy 模式。
- [x] 2.3 审查 setup 中其他现有 toggle 项的实现分支，确认不存在额外的“关闭时覆盖关联值”逻辑，并把必要的保护逻辑抽成一致约束。

## 3. 补充回归验证

- [x] 3.1 为 `add` / `modify` / `del` 模板开关补充测试，覆盖“编辑模板 -> 关闭 -> 重新开启”后模板值保持不变。
- [x] 3.2 为 `旧代码处理` 补充测试，覆盖“选择模式 -> 关闭 -> 保存/重载 -> 重新开启”后模式保持不变。
- [x] 3.3 运行 setup / config 相关测试集，确认其他开关项没有引入新的回归。
